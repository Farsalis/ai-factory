# Copy to .env and fill in. Do not commit .env.
# Required for gated Hugging Face models (e.g. some Llama / Gemma checkpoints).
HF_TOKEN=

# Hugging Face cache lives in the named volume mounted at /hf-cache.
# HF_HOME=/hf-cache
